import styled from 'styled-components';

export const Header = styled.header`
  width: 100%;
  font-size: 20px;
  position: relative;
  text-align: center;
  padding: 6px 0;
`;

export const Link = styled.a`
  position: absolute;
  right: 8px;
`;
